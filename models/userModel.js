const mongoose = require('mongoose');

const UserCollection = new mongoose.Schema({
    name: {
        required: true,
        type: String
    },
    age: {
        required: true,
        type: Number
    }
})

module.exports = mongoose.model('uerData', UserCollection)